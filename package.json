{
  "name": "investep-app-api",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "API REST de Investep App (Hono + Cloudflare Workers).",
  "scripts": {
    "dev": "wrangler dev",
    "test": "bun test",
    "lint": "biome check . && tsc --noEmit",
    "format": "biome format --write .",
    "fix": "biome check --write .",
    "typegen": "wrangler types",
    "deploy": "wrangler deploy"
  },
  "engines": {
    "bun": ">=1.3.0"
  },
  "dependencies": {
    "@hono/swagger-ui": "^0.6.1",
    "@hono/zod-openapi": "^1.4.0",
    "@scalar/hono-api-reference": "^0.11.5",
    "@supabase/supabase-js": "^2.108.2",
    "hono": "^4.12.27",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.5.1",
    "@cloudflare/workers-types": "^4.20260623.1",
    "@types/bun": "^1.3.14",
    "typescript": "^6.0.3",
    "wrangler": "^4.103.0"
  }
}
