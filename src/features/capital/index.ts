/**
 * Dominio: CAPITAL — capital del usuario y asignación a brokers con plan de profit.
 * Barrera del feature: re-exporta el router HTTP (`/capital`, `/capital/allocations`).
 */
export { capitalRouter } from "./capital.router";
