/**
 * Dominio: OPERATIONS — journal de operaciones (activos y opciones) por cuenta
 * de bróker. Barrera del feature: re-exporta el router HTTP (`/operations`).
 */
export { operationsRouter } from "./operations.router";
