/**
 * Dominio: PROJECTIONS — serie "Desempeño vs Plan" (interés compuesto por plan).
 * Barrera del feature: re-exporta el router (`/projections`, lectura autenticada).
 */
export { projectionsRouter } from "./projections.router";
